#include <stdio.h>

int main(void)
{
    /*
    char *a, *b;
    a = "abacate";
    b = "uva";
    if(a < b) {
        printf("%s vem antes de %s no dicionario\n", a,b);
    } else {
        printf("%s vem depois de %s no dicionario\n", a,b);
    }
    */
    char *a, *b;
    a = "abacate";
    b = "uva";
    if(a < b) {
        printf("%s vem antes de %s no dicionario\n", a,b);
    } else {
        printf("%s vem depois de %s no dicionario\n", a,b);
    }
    
    return 0;
}